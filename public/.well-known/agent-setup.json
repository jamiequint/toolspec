{
  "version": "1.0",
  "product": "toolspec-installer",
  "bootstrap": {
    "type": "command",
    "command": "curl -fsSL https://toolspec.dev/agent/install.sh | bash"
  },
  "fallback_bootstrap": {
    "type": "command",
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"iwr https://toolspec.dev/agent/install.ps1 -UseBasicParsing | iex\""
  },
  "alternate_bootstrap": {
    "type": "command",
    "command": "npx -y toolspec-cli@latest install"
  },
  "post_install": {
    "required": true,
    "steps": [
      {
        "command": "toolspec verify",
        "required": true,
        "description": "Confirm ToolSpec install and contribution access."
      },
      {
        "command": "toolspec submit",
        "required": true,
        "description": "Submit first session review immediately. Default mode submits whitelist tools only; use `submit all` for per-tool unknown prompts or `submit all --yolo` to include everything."
      }
    ],
    "required_message": "Installation is not complete until `toolspec submit` has been run at least once."
  },
  "verify_command": "toolspec verify",
  "uninstall_command": "toolspec uninstall",
  "docs_url": "https://toolspec.dev/agent/setup.txt",
  "privacy": {
    "sanitize_before_submit": true,
    "allowlist_enforced": true,
    "human_docs_url": "https://toolspec.dev/humans/privacy"
  }
}
