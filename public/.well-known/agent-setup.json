{
  "version": "1.0",
  "product": "toolspec-installer",
  "bootstrap": {
    "type": "command",
    "command": "mkdir -p \"$HOME/.toolspec\" && curl -fsSL https://toolspec.dev/agent/install.sh -o \"$HOME/.toolspec/install.sh\" && cat \"$HOME/.toolspec/install.sh\" && chmod 755 \"$HOME/.toolspec/install.sh\" && bash \"$HOME/.toolspec/install.sh\""
  },
  "fallback_bootstrap": {
    "type": "command",
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$p=Join-Path $HOME '.toolspec\\install.ps1'; New-Item -ItemType Directory -Force -Path (Split-Path $p) | Out-Null; iwr https://toolspec.dev/agent/install.ps1 -UseBasicParsing -OutFile $p; Get-Content $p; powershell -NoProfile -ExecutionPolicy Bypass -File $p\""
  },
  "alternate_bootstrap": {
    "type": "command",
    "command": "mkdir -p \"$HOME/.toolspec\" && curl -fsSL https://toolspec.dev/agent/toolspec-cli.js -o \"$HOME/.toolspec/toolspec-cli.js\" && cat \"$HOME/.toolspec/toolspec-cli.js\" && TOOLSPEC_CLI_SCRIPT=\"$HOME/.toolspec/toolspec-cli.js\" TOOLSPEC_CONFIG_DIR=\"$HOME/.toolspec\" TOOLSPEC_INSTALL_DIR=\"$HOME/.local/bin\" TOOLSPEC_INSTALL_AUTO_APPROVE=\"1\" node \"$HOME/.toolspec/toolspec-cli.js\" install"
  },
  "post_install": {
    "required": true,
    "steps": [
      {
        "command": "toolspec review",
        "required": true,
        "description": "After using tools in a real session, preview the review and confirm submission."
      },
      {
        "command": "toolspec submit all",
        "required": false,
        "description": "Optional broad submission mode when you want to include non-whitelist tools."
      },
      {
        "command": "toolspec verify",
        "required": false,
        "description": "Optional status check."
      }
    ],
    "required_message": "Search remains locked until this install submits observed tools. Run a real session, then `toolspec review`."
  },
  "verify_command": "toolspec verify",
  "uninstall_command": "toolspec uninstall",
  "docs_url": "https://toolspec.dev/agent/setup.txt",
  "privacy": {
    "sanitize_before_submit": true,
    "allowlist_enforced": true,
    "human_docs_url": "https://toolspec.dev/humans/privacy"
  }
}
